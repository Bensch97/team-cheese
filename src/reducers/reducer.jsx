
import { REGISTER, LOGIN } from "../actions";
import * as R from 'ramda';
const initialState= [];

const foodFetchReducer = (state = initialState, action) => {
    switch (action.type) {
        case REGISTER:
            
            break;
    
        default:
            break;
    }
}
export default foodFetchReducer